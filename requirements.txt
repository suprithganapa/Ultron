# ULTRON — backend dependencies
fastapi==0.111.0
uvicorn[standard]==0.30.1
httpx==0.27.0
python-dotenv==1.0.1
pydantic==2.7.4

# Durable cloud memory (used only when DATABASE_URL is a Postgres URL)
psycopg[binary]==3.2.3

# Integrations (Gmail + Calendar). Optional at runtime — only needed if you
# enable those tools.
google-auth==2.35.0
google-auth-oauthlib==1.2.1
google-api-python-client==2.149.0

# Spotify control (optional)
spotipy==2.24.0
